---
import { Constants } from '~/types';
import ArrowLink from './ArrowLink.astro';
import Broadcast from '../icons/Broadcast.astro';


export interface Props {
	title: string;
  href?: string;
	className: string;
	type?: "link" | "button", 
	ariaLabel?: string;
	withArrow?: boolean;
	live?: boolean
}

const { title, href, className, type, ariaLabel, withArrow, live } = Astro.props 
const showLink = type === Constants.LINK ? true : false
---
{
	showLink ? (
		withArrow ? (
			<ArrowLink title={title} />
		) : (
			<a
				href={href}
				class={className}
				rel="prefetch"
				aria-label={ariaLabel || title}>
				<div class="flex justify-center items-center gap-1">
					{title}
					{
						live && <Broadcast className="live" />
					}
				</div>
			</a>
		)
	) : (
		withArrow ? (
			<ArrowLink title={title} />
		) : (
			<button
				class={className}
				aria-label={title}>
				{title}
			</button>
		)
	)
}