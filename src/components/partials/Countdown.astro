---
export interface Props {
  classes?: string;
  date: Date;
}

const { classes, date } = Astro.props
const defaultClasses = "grid grid-flow-col gap-2 text-center auto-cols-max"
const className = defaultClasses + " " + classes
---

<div class={className}> 
  <div class="flex flex-col p-2 rounded-lg bg-white dark:bg-darker shadow-custom">
    <span class="countdown text-2xl">
      <span style="--value:15;" aria-digit="days" class="relative"></span>
    </span>
    days
  </div> 
  <div class="flex flex-col p-2 rounded-lg bg-white dark:bg-darker shadow-custom">
    <span class="countdown text-2xl">
      <span style="--value:10;" aria-digit="hours" class="relative"></span>
    </span>
    hours
  </div> 
  <div class="flex flex-col p-2 rounded-lg bg-white dark:bg-darker shadow-custom">
    <span class="countdown text-2xl">
      <span style="--value:24;" aria-digit="minutes" class="relative"></span>
    </span>
    min
  </div> 
  <div class="flex flex-col p-2 rounded-lg bg-white dark:bg-darker shadow-custom">
    <span class="countdown text-2xl">
      <span style="--value:49;" aria-digit="seconds"  data-date={new Date(date).toLocaleString()} class="relative"></span> 
    </span>
    sec
  </div>
</div>

